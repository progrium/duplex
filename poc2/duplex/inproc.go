package duplex

import (
	"errors"
)

func newPeerListener_inproc(peer *Peer, addr string) (peerListener, error) {
	return nil, errors.New("not implemented")
}

func newPeerConnection_inproc(peer *Peer, addr string) (peerConnection, error) {
	return nil, errors.New("not implemented")
}
